# 保存情報のサーバー同期 設計マニュアル

お気に入り・表示設定・地図の位置をサーバーに預け、複数の端末で同じ状態を再現するための設計です。
**ログイン画面は作りません。**

- 版: 0.7
- 作成: 2026-08-20
- 操作手順（登録・解除）と画面の案: [§5.6〜5.11](#56-追加する画面とボタン)。画面のモックは `mock/sync-ui-preview.html`
- 状態: **未実装**（方針をまとめた段階。実装着手前）
- 対象: この地図UI（`index.html`）と、同じリポジトリに追加する Worker
- 改訂履歴: [§13](#13-改訂履歴)

このファイルは公開リポジトリに置きます。**鍵・トークン・データベースIDなどの秘密値は一切書きません。**
安全性はこの文書を隠すことに依存しません。

---

## 1. 何を解決するのか

現在、保存はすべて表示側の `localStorage` にあります（[§3](#3-保存対象)）。
このため次の3つが起きます。

| 起きること | 例 |
|---|---|
| 端末を変えると消える | 機種変更、PWAの再インストール |
| ブラウザのデータ削除で消える | サイトデータのクリア |
| 端末をまたいで同じ状態にできない | PCで付けたお気に入りがスマホに無い |

このマニュアルは、**サーバー保存**（消えない）と**端末間の同期**（同じ状態）を、
ログイン・パスワード・メールアドレスを一切使わずに実現する方針を定めます。

---

## 2. 全体像

要点は3つです。

1. **ログイン画面を作らない。** 最初に何かを保存した時点で、サーバーが無記名のキーを自動で発行し、
   HttpOnly Cookie に入れます。ユーザーは何も入力しません
2. **`localStorage` が正、サーバーは控え。** 画面は今までどおり即座に立ち上がり、同期は裏で回します
3. **端末を足すときだけ「引き継ぎ券」を使う。** QRを表示している間だけ有効なコードで、
   参加した端末は設定画面の一覧から**いつでも失効**させられます

```
[端末A] localStorage ──即時描画
            │  裏で同期（デバウンス）
            ▼
     Worker (/api/*) ── D1（お気に入り・設定・地図位置）
            ▲
            │  引き継ぎ券（QRを表示している間だけ）で参加
[端末B] localStorage
```

### 2.1 「ログイン」を作らない理由

必要なのは「この保存領域は自分のものだ」と示す秘密が1つあることだけで、
ID・パスワード・メールアドレスは要りません。無記名の128ビット乱数で足ります。
これにより次が同時に得られます。

- ユーザーの入力操作がゼロになる（初回から即サーバー保存）
- パスワードの保管・再設定・強度の問題が発生しない
- 遅いハッシュ（PBKDF2 等）が不要になり、Workers 無料枠の **CPU 10ms/呼び出し** の制約に当たらない
- サイトは公開のまま。URLを知られても、Cookie が違えば別の保存領域になる

---

## 3. 保存対象

2026-08-20 時点の `index.html` にある保存キーです（行番号は同日時点）。

| キー | 内容 | 現在の書き込み契機 | 該当行 | サーバーでの扱い |
|---|---|---|---|---|
| `favorites` | お気に入り（4枠の名前 ＋ `items{key:{mk,at}}`） | 付け外しの都度 | 8288, 8324-8348 | **共有** |
| `settingsState` | 種別フィルタ・ラベル・部門OFF・取得元表示 | 設定変更時 | 5685 | **共有** |
| `mapTileSource` | タイル配信元 | 変更時（リロードを伴う） | 5707 | **共有** |
| `hotelRadius` / `hotelSort` | 宿検索の条件 | 変更時 | 5717, 5721 | **共有** |
| `travelMode` | 移動手段 | 変更時 | 5725 | **共有** |
| `nearbyTypeFilterOff` | 周辺検索のタイプOFF | 変更時 | 5658 | **共有** |
| `mapState` | lng / lat / zoom / bearing / pitch | `moveend`・`zoomend` の毎回 | 4589 | **端末ごと** |

`mapState` だけを端末ごとに分けるのは、4586行のコメントどおり「PWA再起動時の復元用」で、
端末によって意味が違うためです。共有すると、PCで見ていた位置にスマホの起動位置が引っ張られます。
サーバーには保存する（端末が壊れても復元できる）が、領域を端末ごとに分けます。

単発キー5本（`mapTileSource` / `hotelRadius` / `hotelSort` / `travelMode` / `nearbyTypeFilterOff`）は、
サーバー上では `prefs` 1件にまとめて往復を減らします。

---

## 4. 同期の動き

### 4.1 なぜ「ローカルが正」なのか

- 起動時、2214・2218・2221行は `localStorage` を**同期的**に読んで地図の初期位置とフィルタを決めています。
  ここをサーバー読みに変えると、初期描画が待たされるか、一瞬ずれた地図が出ます
- `sw.js` の Service Worker により、オフラインでも起動する前提です。サーバー必須にするとこれを壊します

### 4.2 起動時

1. 今までどおり `localStorage` から即座に描画する
2. 裏で `GET /api/state` を投げる
3. サーバーのほうが新しければ `localStorage` を上書きし、お気に入りバッジと設定を再描画する
4. `mapState` は起動後に動かすと操作の邪魔になるため、**反映は次回起動から**とする

### 4.3 変更時

| キー | サーバーへ送る頻度 |
|---|---|
| `favorites` | 都度（2秒デバウンス） |
| `settingsState` / `prefs` | 都度 |
| `mapState` | **30秒に1回、＋ `pagehide` / `visibilitychange` 時**（毎 `moveend` では送らない） |

オフラインや失敗時は送信待ちを `localStorage` に積み、次にオンラインになったときにまとめて流します。

### 4.4 競合（同じキーを別々の端末で変えた場合）

初版は**キー単位の後勝ち**とします。サーバーは各キーに `rev`（更新回数）を持ち、
クライアントは自分が読んだ `rev` を添えて送ります。食い違えば 409 を返し、クライアントは
サーバー側を取り込んでから再送します。

`favorites` は項目ごとに `at`（最終操作時刻）を持っているため、将来は項目単位のマージへ拡張できます。
ただし**削除を正しく伝えるには墓標（tombstone）が必要**なので、初版では見送ります。

### 4.5 最初の持ち上げ

初回に保存領域を作るとき、**サーバーが空の場合に限り**、その端末の `localStorage` の内容を
そのままサーバーへ上げます。これが無いと、同期を始めた瞬間に手元のお気に入りが空で上書きされます。

---

## 5. 端末の参加と失効

### 5.1 引き継ぎ券

| 項目 | 仕様 |
|---|---|
| 中身 | 128ビットの乱数（サーバーは SHA-256 のみ保持） |
| 表示 | 設定画面の「別の端末を追加」を押した時**だけ**。QRと文字列 |
| 有効期間 | **QRを表示している間だけ。**画面を閉じる・別画面へ移ると即失効（放置対策に最大10分で自動失効） |
| 使える台数 | **表示中なら何台でも。**4台を続けて読ませられる |
| 参加の可視化 | 参加した端末が QR 画面にその場で積み上がる。端末一覧にも即載る |
| PIN | **無し** |

**キー本体は画面に出しません。**HttpOnly Cookie にだけ置き、JavaScript からも読めません。

> **重要**: 引き継ぎ券は「入室券」です。有効期間の制限がかかるのは**参加する瞬間だけ**で、
> いったん参加した端末は、失効させるまでずっと同期し続けます。

#### 4台で共有する場合

**発行は1回だけです。**1台目でQRを出し、開いたまま残り3台に順に読ませます。
読ませるたびに画面へ「iPhone が参加しました」と積み上がるので、4台そろったら閉じます。

5台目を後日足すときは、**参加済みのどの端末からでも**同じ操作で出せます。1台目を持ち出す必要はありません。

#### なぜ「1回きり」ではなく「表示中だけ」なのか

1回きりにすると、4台では発行を3回繰り返すことになり、実務に合いません。
「表示中だけ」にすると、券が有効なのは**本人がその画面を見ている間**に限られるため、
放置された券が後から使われることがなくなります。窓は時間ではなく操作で閉じます。

#### PIN を置かない理由

PIN は「離れた場所からQRを撮られる」対策でした。有効期間が画面を見ている間だけになったことで、
撮る側もその場に居る必要があり、PIN が守る範囲はほぼ無くなります。
肩越しに撮られる経路は残りますが、参加が起きればその場で画面と端末一覧に現れるため、
[§5.2](#52-端末一覧設定画面) から即座に失効させられます。

### 5.2 端末一覧（設定画面）

| 表示・操作 | 内容 |
|---|---|
| 端末名 | User-Agent から自動生成（「iPhone / Safari」程度）。改名可 |
| 参加日時・最終アクセス | いつ入って、いつまで使っていたか |
| この端末を外す | その1台だけ失効 |
| 自分以外を全部外す | 心当たりの無い端末を見つけたときの一括操作 |

失効はサーバー側のセッション行を消すだけで成立します。端末側の協力は要りません。

### 5.3 共有のしかたを選ぶ

| | 仕組み | 向く場面 | 採否 |
|---|---|---|---|
| A 無期限で参加 | 失効させるまで同期 | 自分のスマホ＋PC | **採用（既定）** |
| B 期限つき参加 | 7日・30日などで自動的に同期停止 | 借りた端末 | **不採用**（2026-08-20 決定） |
| C 読み取り専用で参加 | 同期はするが書き込みAPIを拒否 | 家族に見せる | **不採用**（2026-08-20 決定） |
| D 同期しない複製 | その時点の状態をファイル／QRで渡す。以後は別々 | 今のお気に入りだけ渡す | **採用** |

B・C を作らないため、`devices` の `mode` と `expires_at`（[§7.2](#72-テーブル)）は使いません。
列は将来のために残しますが、値は `'rw'` と `NULL` で固定です。

**A と D を別々のボタンとして最初から用意します。**「端末を追加（以後ずっと同期）」と
「今の状態を渡す（以後は別々）」を分けておけば、後者を選んだ相手とは何も共有され続けません。

### 5.4 D（同期しない複製）とバックアップ

`index.html` には既にお気に入りTSVの書き出しがあります（8842行付近、`createObjectURL` → `a.download`）。
同じ導線に「全設定をJSONで書き出し／読み込み」を足します。サーバーも認証も使いません。

QRに丸ごと詰める形も同じ仕組みで作れます。実測サイズは [§10](#10-実測データ) を参照してください。

### 5.5 取り消せないこと

**失効させても、その端末の `localStorage` に残ったコピーは消えません。**
同期が止まるだけです。次にオンラインで API が 401 を返した時点でローカルを消す実装はできますが、
その端末を二度とオンラインで開かなければ消えません。
**一度渡したデータを後から取り消すことは、どの方式でも原理的にできません。**

### 5.6 追加する画面とボタン

画面のモックは `mock/sync-ui-preview.html` にあります（SE3 375×667 の実寸、クラス名と色は
`index.html` の実物から写しています）。

**常設のボタンは1つも増やしません。**メニューパネルは既にビューを切り替える作り
（周辺検索・城主・城主詳細・都道府県・遺構・お気に入り一覧・書き出し）なので、同期もその枠組みに載せます。

| 追加するもの | 置き場所 | 中身 |
|---|---|---|
| 設定の「同期」区画 | `#nearby-settings-view` の「メニュー表示」と「お気に入りの名前」の間 | 状態1行 ＋ ボタン2つ（端末を追加／端末の管理）＋ 書き出し1行 |
| ビュー「端末を追加」 | メニューパネル内の第12ビュー | QR ＋ 手入力コード ＋ 参加した端末の並び |
| ビュー「端末の管理」 | メニューパネル内の第13ビュー | 端末の一覧 ＋ 外す ＋ 保存領域の削除 |

「お気に入りの名前」より**上**に置きます。名前の欄は文字入力なので最下段に固定されており
（1767行のコメント：上に置くと入力でキーボードが出たとき下の行が隠れる）、
ボタンだけの同期区画はその手前に入ります。

#### ボタンの文言と色

| ボタン | 見た目 | 理由 |
|---|---|---|
| 端末を追加 | `.fxp-btn.primary`（`--ui-blue #4285F4`） | この区画で最も使う操作。書き出しビューの主ボタンと同じ扱い |
| 端末の管理 | `.fxp-btn.sub`（`#f2f4f7`） | 参照が主で、押す回数が少ない |
| 外す／保存領域を削除する | `.fxp-btn.danger`（地 `#fdeceb` ／ 字 `#C62828`） | 取り消せない操作だけ赤。**この赤が出るのはパネルの中だけ**で地図には出ないため、種別色との判別は問題にならない（お気に入り入口の `#FFC107` を「パネルの中だけ」と決めた1662行の扱いと同じ） |
| 閉じる（QRを無効にする） | `.fxp-btn.sub` | 「閉じる＝券が切れる」ことを文言に入れる。押し忘れても最大10分で切れる |

#### コードの渡し方

| 手段 | 中身 | 備考 |
|---|---|---|
| QR | `https://（サイト）/#join=（128ビットのコード）` | **標準のカメラで読むだけで参加画面まで進む**ので、アプリ内にカメラ機能を作りません。フラグメント（`#`）はサーバーへ送られないため、アクセスログにコードが残りません。参加後は `history.replaceState` で履歴からも消します |
| 手入力 | 8文字（`K7QP-3M9X` 形式、40ビット相当） | QRを読めない端末（PC）向け。有効なのは画面を開いている数分間だけで、レート制限も掛かるため、40ビットを当てにいくことは現実的にできません |

表示側で新しく要る外部部品は**QRの生成だけ**で、**unpkg から読み込みます**（2026-08-20 決定）。
`index.html` は既に `maplibre-gl@3.6.2` と `open-location-code` を unpkg から読んでおり、
`sw.js` の `CACHEABLE_CDN` も unpkg なので、同じ入れ方で足せます。自前実装（約150行）は採りません。

### 5.7 手順：1台目

**操作は不要です。**お気に入りを1つでも付けた時点で、サーバー側の保存領域が自動で作られます。
設定の「同期」区画に「この端末は同期しています」と出ていれば、それが済んだ状態です。

### 5.8 手順：2台目以降を登録する

4台で共有する場合も、**QRの発行は1回だけ**です。

| # | どの端末で | 操作 | 画面に出ること |
|---|---|---|---|
| 1 | 1台目 | 設定 →「端末を追加」 | QRと8文字のコード。「この画面を閉じると無効になります」 |
| 2 | 2台目 | 標準のカメラでQRを読み、開いたURLを開く | 「この端末を追加しますか？」の確認 |
| 3 | 2台目 | 端末名を確認し、手元のお気に入りを**持ち込んで統合**するか**捨てて合わせる**かを選び「追加する」 | 地図に戻り、同期済みになる |
| 4 | 1台目 | **②の画面を開いたまま**、3台目・4台目にも読ませる | 「参加した端末」に積み上がる |
| 5 | 1台目 | 「閉じる（QRを無効にする）」 | 以後そのコードは使えない |

- QRが読めない端末では、手順2の代わりに設定 →「端末を追加」→ 8文字を入力します
- 5台目を後日足すときは、**参加済みのどの端末からでも**同じ操作で出せます。1台目を持ち出す必要はありません
- 手順3の選択は、**その端末に既にお気に入りがある場合だけ**聞きます。空なら黙って合わせます

### 5.9 手順：解除する

| やりたいこと | 操作 | 結果 |
|---|---|---|
| 他の端末を切り離す | 設定 →「端末の管理」→ その行の「外す」→ 確認で「外す」 | その端末は次の通信から同期停止。手元のお気に入りは残る |
| 心当たりの無い端末をまとめて切る | 「この端末以外をすべて外す」 | 自分以外の全端末が同期停止 |
| この端末だけ抜ける | 「この端末の同期をやめる」 | この端末が同期をやめる。他の端末はそのまま共有を続ける |
| 共有そのものをやめる | 「保存領域を削除する」 | サーバー上の内容と全端末の結び付きが消える。各端末のお気に入りは残る |

確認画面では、**その端末に残るものと残らないもの**を必ず書いてから押させます
（[§5.5](#55-取り消せないこと) のとおり、外してもコピーは消えないため）。

### 5.10 手順：書き出し／読み込み

サーバーも同期も使わない経路です。設定 →「書き出し／読み込み」から、お気に入りと設定を
JSON 1ファイルに落とし、別の端末で読み込みます。**読み込んだ端末とは同期しません。**
バックアップと、同期したくない相手への受け渡しの両方に使います。

### 5.11 困ったとき

| 症状 | 対処 |
|---|---|
| QRを読んだのに「無効です」と出る | 1台目で画面を閉じたか、10分を過ぎています。もう一度「端末を追加」を押します |
| 端末一覧に見覚えのない端末がある | その行の「外す」。心当たりが全く無ければ「この端末以外をすべて外す」 |
| 2台目のお気に入りが消えた | 手順3で「捨てて合わせる」を選んだ場合の想定どおりの動きです。事前に §5.10 で書き出しておけば戻せます |
| 同期しているのに片方だけ古い | 変更は最大30秒遅れて送られます（地図位置は画面を閉じたときも送信）。オフラインなら次にオンラインになった時点で流れます |
| 全部やり直したい | 「保存領域を削除する」→ 各端末で改めて1台目から登録し直します |

### 5.12 アプリを更新したときに登録はどうなるか

**通常の改訂（`index.html` や Worker のコードを直してプッシュ）では、登録し直す必要はありません。**
登録の実体は次の2つで、どちらもデプロイで触られないためです。

| 登録の実体 | 置き場所 | デプロイで消えるか |
|---|---|---|
| セッション | 端末の `__Host-sid` Cookie（HttpOnly） | 消えない（ブラウザ側にある） |
| 端末の結び付き | D1 の `devices` 行 | 消えない（`database_id` が同じ限り、同じデータベースを指す） |

`sw.js` の `VERSION` を上げた場合も、`activate` が行うのは `caches.delete()` だけで
（実ファイルで確認済み）、Cookie と localStorage には触れません。

#### やり直しになるのはこの5つだけです

| 操作 | 影響 | 避け方 |
|---|---|---|
| Worker 名の変更、独自ドメインへの移行、`workers.dev` サブドメインの変更 | **ホストが変わる＝全端末で再登録**（Cookie はホスト限定のため） | 独自ドメインを当てるなら**同期を実装する前**に済ませる。後から移す場合は、旧ホストでQRを出して新ホストで読ませる移行を1回流す |
| Cookie 名の変更 | 全端末で再登録 | 変えない。どうしても変えるなら、旧名を読んで新名で打ち直す処理を1回入れる |
| D1 を作り直す・`database_id` を差し替える | **保存内容と全端末の結び付きが消える** | 作り直さない。列の追加は `ALTER TABLE` で行い、テーブルの作り直しをしない |
| `Max-Age`（1年）を超えて一度も開かなかった端末 | その端末だけ期限切れ | **アクセスのたびに Cookie を打ち直す**（期限を毎回1年に延ばす）。実装に含める |
| 利用者がサイトデータを削除・PWAを再インストール・別ブラウザで開く | その端末だけ再登録 | 設計上の弱点として残る（[§5.11](#511-困ったとき)） |

#### Safari のストレージ削除には強くなります

Safari は、7日間そのサイトを操作しないと**スクリプトから書けるストレージ**
（localStorage・IndexedDB・SessionStorage・Service Worker の登録とキャッシュ）を削除しますが、
**HttpOnly の Cookie はこの対象外**です（WebKit の公式記事で確認）。
つまり localStorage が消えても登録は生き残り、次に開いたときサーバーから復元されます。
ホーム画面に追加した Web アプリは Safari とは別のカウンタを持ち、使っていればリセットされます。

---

## 6. 安全性の方針

| 対象 | 方針 | 根拠 |
|---|---|---|
| セッションCookie | `__Host-sid=…; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=31536000`。**`Domain` は付けない**（ホスト限定） | PWAで毎回切れると使い物にならないため長期。代わりに端末一覧から失効できる。`__Host-` は `Secure` と `Path=/` を要求し `Domain` を禁じるため、ホスト限定であることをブラウザ側で担保できる（MDN） |
| Cookie の到達範囲 | 上記のとおりホスト限定にする。`Domain` を付けてはならない | サイトは `*.workers.dev` の1ホストで動いている。`workers.dev` は **Public Suffix List に登録されている**ので（2026-08-20 に一覧を取得して確認）、`Domain=workers.dev` はブラウザに拒否される一方、`Domain=<アカウント>.workers.dev` は通ってしまい、**同じアカウントの他の Worker とCookieを共有することになる** |
| CSRF | `SameSite=Lax` を**明示指定**。加えて書き込みAPIに独自ヘッダを必須にする | Lax は「安全なメソッドのトップレベル遷移」以外にCookieを送らない。POST / PUT / DELETE は対象外（MDN）。ただし *Laxが既定として適用される場合に限り* 2分間POSTを許す緩和があるため、明示指定が要る |
| 保存する秘密 | セッショントークンも引き継ぎ券も、**SHA-256 のハッシュだけ**を保存する | 生の値が漏れても再利用できない |
| 総当り | 引き継ぎ券は128ビットの乱数で、有効なのは画面を開いている数分間だけのため、当てにいくことは現実的にできない。`/api/join` `/api/enroll` には全体のレート制限だけ掛ける | [§5.1](#51-引き継ぎ券) |
| Worker のソース | `.assetsignore`（`.gitignore` と同じ書式）で配信対象から除外する | `[assets] directory = "./"` はリポジトリ直下を丸ごと配信するため |
| 秘密値の置き場所 | ダッシュボードの Secrets（暗号化変数）。コードにもこの文書にも書かない | リポジトリは公開 |
| 無操作の訪問者 | **ページを開いただけでは保存領域を作らない。**最初の保存が発生したときにだけ発行する | クローラー等で不要な行が増えるのを防ぐ |

### 6.1 漏れたときに何が起きるか

| | 漏れる窓 | できること |
|---|---|---|
| 引き継ぎ券（この設計） | QRを表示している間だけ | その間に読み取られた場合のみ読み書き。参加はその場で画面と端末一覧に出るので、気付いて失効させられる |
| QR・URLに丸ごと（D） | 撮られた時点で中身が全部読める | **読みのみ**。以後の更新は追えず、書き換えもできない |
| 書き出しファイル（D） | ファイルを渡さない限り無し | ファイルを持つ人だけ読める |

読まれる情報はお気に入り・表示設定・最後に見ていた地図位置だけで、
氏名・メール・決済情報はどこにも持ちません。
ただし `mapState` は最後に見ていた地点（自宅周辺の可能性）、お気に入りは行動範囲を示すため、
無視してよい情報ではないという前提で扱います。

---

## 7. サーバー構成

### 7.1 なぜ Workers ＋ D1 なのか

現在の `wrangler.toml` は `name` と `[assets] directory = "./"` だけで `main` がありません。
`[assets]` と `main` は Workers 専用のキーで、Pages は `pages_build_output_dir` が必須です。

**このリポジトリが Workers Builds でデプロイされていることは確認済みです。**
GitHub の最新コミットに付くチェックが `Workers Builds: geopenguin`（提供元 *Cloudflare Workers and Pages*）で、
その参照先が `.../workers/services/view/geopenguin/production/builds/...` だからです（2026-08-20 に確認）。
なお、同リポジトリには 2026-05-13 までの GitHub Pages へのデプロイ記録も残っていますが、
`https://jyushock.github.io/geo-v5r7w2mBq8/` は現在 404 で、配信していません。

配信は **`*.workers.dev` の1ホストだけ**で、独自ドメインは当てていません
（2026-08-20 に HTTP 200・`server: cloudflare`・`<title>カスタムマップ</title>`・556KB を確認）。
これは Cookie の指定（[§6](#6-安全性の方針)）と、Google ID を採る場合の登録（[§11.1](#111-google-id-でのログインを後から足す場合)）に効いてきます。

つまり現状は**Workerスクリプトの無い静的アセットのみの Worker** で、
`main` を足せば**同じリポジトリ・同じプッシュ契機のまま** API を生やせます。
アセットに一致しないURL（`/api/*`）は既定で Worker に渡るため、追加のルーティング設定も要りません。

保存先は D1 です。KV は**書き込みが無料枠で 1,000/日**しかなく、地図位置の保存と相性が悪いためです
（D1 は 10万行/日）。

### 7.2 テーブル

```sql
CREATE TABLE users (
  id         TEXT PRIMARY KEY,          -- 乱数
  created_at INTEGER NOT NULL
);

CREATE TABLE devices (
  id           TEXT PRIMARY KEY,        -- 乱数
  user_id      TEXT NOT NULL,
  token_hash   TEXT NOT NULL UNIQUE,    -- セッショントークンの SHA-256（hex）
  name         TEXT NOT NULL DEFAULT '',
  ua           TEXT NOT NULL DEFAULT '',
  mode         TEXT NOT NULL DEFAULT 'rw',   -- 'rw' 固定（C は不採用。列だけ残す）
  created_at   INTEGER NOT NULL,
  last_seen_at INTEGER NOT NULL,
  expires_at   INTEGER                  -- NULL 固定（B は不採用。列だけ残す）
);

CREATE TABLE handovers (
  code_hash  TEXT PRIMARY KEY,          -- 引き継ぎ券の SHA-256
  user_id    TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  expires_at INTEGER NOT NULL           -- 最大10分。通常はQRを閉じた時点で行ごと削除する
);

CREATE TABLE states (
  user_id    TEXT NOT NULL,
  device_id  TEXT NOT NULL DEFAULT '',  -- '' ＝ 全端末で共有、それ以外＝その端末専用
  key        TEXT NOT NULL,             -- favorites / settingsState / prefs / mapState
  value      TEXT NOT NULL,             -- JSON
  rev        INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  PRIMARY KEY (user_id, device_id, key)
);

CREATE INDEX idx_devices_user   ON devices  (user_id);
CREATE INDEX idx_handovers_exp  ON handovers(expires_at);
```

`device_id` を NULL ではなく空文字にするのは、SQLite が主キー中の NULL を互いに別物として扱い、
一意性が担保されないためです。`states` は主キーの先頭が `user_id` なので、別の索引は要りません。

### 7.3 API

| メソッド・パス | 役割 |
|---|---|
| `POST /api/enroll` | セッションが無い状態で**最初の保存**が起きたときだけ呼ぶ。`users` と `devices` を作り Cookie を配る |
| `GET /api/state` | 共有分＋この端末分をまとめて取得（`rev` 付き） |
| `PUT /api/state` | 変更キーをまとめて保存。`rev` 不一致は 409 |
| `POST /api/handover` | 引き継ぎ券を発行（QRを開いたとき。最大10分） |
| `DELETE /api/handover` | 引き継ぎ券を破棄（QRを閉じたとき。`pagehide` でも送る） |
| `POST /api/join` | 引き継ぎ券で新端末を参加させ、その端末のCookieを配る |
| `GET /api/devices` | 端末一覧。QR表示中は数秒ごとに呼び、参加した端末をその場に出す |
| `DELETE /api/devices/{id}` | 失効 |

D1 は**1回のWorker呼び出しあたり50クエリ**まで（無料）なので、
1リクエストで扱うキーは全部合わせても数件に収めます。

---

## 8. 費用と無料枠

| 資源 | 無料枠 | この用途での消費 |
|---|---|---|
| Worker リクエスト | 100,000/日（**静的アセットへの要求は無料・無制限**） | API のみが消費。1人1日あたり数百 |
| Worker CPU | **10ms/呼び出し** | SHA-256 1回だけなので余裕（[§12](#12-未確認事項)に実測の宿題） |
| D1 | 読み 500万行/日、書き 10万行/日、5GB、無料は10DB・1DBあたり500MB | 1ユーザー数行 |
| KV | 書き込み 1,000/日 | **不採用**（[§7.1](#71-なぜ-workers--d1-なのか)） |

**追加費用は発生しない見込みです。**

---

## 9. 導入手順

ローカルに Node / wrangler を入れる必要はありません。ダッシュボードとプッシュだけで完結します。
**シークレット（暗号化変数）の登録は不要です。**無記名キー方式では、アプリ側が持つ秘密値が無いためです。

1. **D1 を作る**
   Cloudflare ダッシュボード → D1 → Create Database → 名前を付けて作成。
   名前は Worker と対応が付く `geopenguin-sync` を推奨。Location hint は Asia-Pacific を選ぶ
2. **テーブルを流す**
   作成したデータベースを開き **Console** タブに [§7.2](#72-テーブル) の SQL を貼って Execute。
   実行後、**Tables** に `users` / `devices` / `handovers` / `states` の4つが並ぶ
3. **バインディングを書く**
   `wrangler.toml` に追記する（`database_id` は手順1の画面からコピー）
   ```toml
   main = "worker/index.js"

   [[d1_databases]]
   binding = "DB"
   database_name = "<作成した名前>"
   database_id = "<ダッシュボードに出るID>"
   ```
4. **配信対象から Worker を外す**
   リポジトリ直下に `.assetsignore` を置き、`worker/` を書く
5. **プッシュする**
   いつもどおり push すれば自動デプロイされる
6. **確認する**
   お気に入りを1つ付け、ダッシュボードの Console で `SELECT * FROM states;` に行が入ることを見る

`database_id` は秘密ではありません。`wrangler.toml` に書いて公開リポジトリへコミットする前提の値で、
これ単体では誰もデータベースへ触れません（アクセスには Cloudflare アカウントの権限が要ります）。
Cloudflare の公式手順でも設定ファイルに書く形で案内されています。

---

## 10. 実測データ

`data.js` / `castle.js` / `michi.js` の実データからお気に入りキーを組み立てて測った値です
（素材の件数は 食べログ 7,369 / 城 23,516 / 道の駅 1,234 で、生成側 README の実測と一致）。
gzip は `CompressionStream('gzip')` 相当です。測定日 2026-08-20。

| 内容 | JSON | gzip後 | base64url |
|---|---|---|---|
| **設定7キーのみ**（お気に入り抜き） | 453 B | 327 B | **436 文字** |
| ＋お気に入り 50件 | 3,547 B | 911 B | 1,215 文字 |
| ＋100件 | 7,061 B | 1,620 B | 2,160 文字 |
| ＋200件 | 14,016 B | 3,091 B | 4,122 文字 |
| ＋500件 | 34,952 B | 7,451 B | 9,935 文字 |
| ＋1,000件 | 69,536 B | 13,816 B | 18,422 文字 |
| ＋3,000件 | 209,345 B | 38,314 B | 51,086 文字 |

QRコードの上限は ISO/IEC 18004 で**バイナリ 2,953バイト**（版40・誤り訂正L）です。したがって、

- **設定だけなら QR 1枚に余裕で入ります**（436文字）
- お気に入り込みでも **100件程度までは1枚に収まります**
- 200件を超えると QR には入らず、URL や ファイルでの受け渡しになります

`CompressionStream` は MDN で「Baseline widely available（2023年5月以降）」です。

---

## 11. 採用しなかった案

| 案 | 却下の理由 |
|---|---|
| ID＋パスワードのログイン | 画面と実装が最も増える。PBKDF2 の反復回数が無料枠の CPU 10ms に収まるかも未確認 |
| Cloudflare Access | 自作コードは0だが、**ホスト単位で認証がかかり、誰でも地図を見られる状態でなくなる**。無料は50ユーザーまで |
| Google ID でのログイン | 初版では見送るが、**後から足せる**。理由と手間の内訳は [§11.1](#111-google-id-でのログインを後から足す場合) |
| 設定JSONをリポジトリに同梱 | このリポジトリは **public**（GitHub API で確認：HTTP 200）。お気に入りが誰でも読める |
| `navigator.storage.persist()` だけで済ませる | 容量逼迫時の削除を防ぐだけで、サイトデータ削除・端末変更には無力。MDN も「ブラウザ固有の規則により要求が尊重されないことがある」としている。補助としては入れてよい |

### 11.1 Google ID でのログインを後から足す場合

「Sign in with Google」を使うと、ボタン1つで ID トークン（JWT）が返り、サーバーはそれを検証して
`sub`（Googleの利用者ID）で保存領域を引き当てます。**リダイレクトもリフレッシュトークンの管理も要りません**
（認証と認可が分離されており、サインインでは ID トークンだけが返るため）。

#### コード量は増えない

| | 内容 |
|---|---|
| **不要になる** | 引き継ぎ券の発行・QR表示、`/api/handover`、`/api/join`、`/api/enroll` の自動発行 |
| **必要になる** | ログインボタンの設置、ID トークンの検証（署名・`aud`・`iss`・`exp`）、`g_csrf_token` の照合、`users` を `google_sub` で引く |
| **変わらない** | `states` テーブル、同期の動き、競合解決、`mapState` の端末別、書き出し／読み込み |

検証は `https://www.googleapis.com/oauth2/v3/certs`（JWK）を取得して WebCrypto で行えます。
Google はクライアントライブラリの利用を推奨していますが、汎用のJWT検証で実装しても構わないと明記されています。

#### 実際の手間は Google Cloud Console 側にあります

| 項目 | 内容 |
|---|---|
| OAuth クライアントID | 作成し、**承認済みJavaScript生成元**にサイトのオリジンを登録する。**オリジンが増減するたびに登録の変更が要る**（`workers.dev`、独自ドメイン、プレビューURL） |
| 公開ステータス「テスト」 | **テストユーザー100人まで**。かつ**認可は同意から7日で失効する** |
| 公開ステータス「本番」 | 人数制限は無くなる。非機密スコープ（`openid` / `email` / `profile`）だけなら**アプリ審査は不要**。ただし外部の本番アプリでは**プライバシーポリシーのURLが必須** |
| ブランド検証 | 同意画面にアプリ名とロゴを出したい場合のみ必要 |
| FedCM | ボタン方式では任意（`use_fedcm_for_button`）。One Tap を使うなら移行作業が要る |

#### 費用

Cloudflare 側は変わらず無料枠に収まります。Google 側で課金の案内があるのは
Identity Platform（MAU課金）や Cloud Identity（$7.2/月・1ユーザー）といった**別製品**です。
「Sign in with Google は無料」と明記した公式ページは確認できていません（[§12](#12-未確認事項)）。

#### 引き換えに変わること

- 利用者に**Googleアカウントが必須**になる
- サーバーが**メールアドレス（または `sub`）を持つ**ことになり、[§6.1](#61-漏れたときに何が起きるか) の
  「氏名・メール・決済情報はどこにも持たない」が成り立たなくなる
- 代わりに、引き継ぎ券をQRで渡す運用と、それに伴う [§5](#5-端末の参加と失効) の危険が丸ごと消える

#### 後付けの手順

`users` に `google_sub TEXT UNIQUE` を1列足し、ログイン時にその行へ既存の `user_id` を結び付けるだけです。
`devices` と `states` は変更不要のため、**無記名キーで運用を始めた後で、いつでも足せます。**

---

## 12. 未確認事項

| 事項 | 状態 |
|---|---|
| `*.workers.dev` を Google の承認済みドメインとして登録できるか | **未確認**（§11.1 を採る場合のみ関係する）。公開ステータスを「本番」にすると承認済みドメインの所有確認を求められるが、`workers.dev` は自分の持ち物ではない。独自ドメインが要る可能性がある |
| SHA-256 1回が CPU 10ms に収まるか | **未実測**（収まる見込み。実装後に計測する） |
| Cloudflare の Rate Limiting Rules を無料枠で使えるか | **未確認**。使えない前提で、回数制限は D1 のカウンタで実装する |
| 「Sign in with Google」自体が無料か | **未確認**。無料と明記した公式ページを確認できていない。料金体系が案内されているのは Identity Platform / Cloud Identity という別製品 |
| ID トークンの RS256 検証が CPU 10ms に収まるか | **未実測**（§11.1 を採る場合） |
| 公開ステータス「テスト」の7日失効が、ID トークンのみのサインインにも及ぶか | **未確認**。本番へ公開すれば論点自体が消える |

---

## 13. 改訂履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 0.7 | 2026-08-20 | §5.12 を追加。アプリ改訂で登録が残ること、やり直しになる5つの操作、Safari のストレージ削除との関係 |
| 0.6 | 2026-08-20 | 配信ホストが `*.workers.dev` の1つで独自ドメインが無いことを確認し、Cookie を `__Host-` 付きのホスト限定に確定（`workers.dev` が Public Suffix List にあるため）。§5.3 の B・C を不採用に決定 |
| 0.5 | 2026-08-20 | デプロイが Workers Builds であることを GitHub のチェック実行から確認し、§7.1 に根拠を記載。§12 の該当する未確認事項を削除 |
| 0.4 | 2026-08-20 | §5.6〜5.11 を追加。追加する画面とボタン、登録・解除・書き出しの手順、困ったときの対処。画面のモック `mock/sync-ui-preview.html` を作成 |
| 0.3 | 2026-08-20 | 引き継ぎ券を「5分・1回きり＋任意のPIN」から「QRを表示している間だけ・台数無制限・PIN無し」に変更。4台を発行1回で追加できるようにするため |
| 0.2 | 2026-08-20 | §11.1 を追加。Google ID でのログインを「実装が重いので却下」から「後から足せる選択肢」に改めた（公式資料で手間と費用を確認し直したため） |
| 0.1 | 2026-08-20 | 初版。方針をまとめた段階で、実装は未着手 |
